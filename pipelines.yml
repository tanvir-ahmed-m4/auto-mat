resources:
  - name: auto-mat-git-repo
    type: GitRepo
    configuration:
      gitProvider: AutoMatGithub
      path: ric03uec/auto-mat
    branches:
      include: pipelines
      
pipelines:
  - name: auto-mat

  steps:
    - name: step_1
      type: Bash
      configuration:
        inputResources:
          - name: auto-mat-git-repo
      execution:
        onExecute:
          - echo "Goodbye World!"
          
    - name: step_2
      type: Bash
      configuration:
        inputSteps:
          - name: step_1               # Execute this step after the prior step
      execution:
        onExecute:
          - echo "Goodbye World!"
